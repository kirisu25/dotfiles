#!/bin/bash

./configure \
    --with-features=huge \
    --enable-multibyte \
    --enable-cscope \
    --enable-python3interp \
    --enable-luainterp \
    --with-x \
    --enable-perlinterp \
    --enable-rubyinterp \
    --enable-fontset \
    --enable-xim \
    --enable-terminal \
    --enable-fail-if-missing \
