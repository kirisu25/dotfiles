{
    user.mutableUsers = false;

    users.users.giulia = {
        isNormalUser = true;
        home = "/home/giulia";
        extraGroups = [ "wheel", "networkmanager" ];
        hashedPassword = "$6$452dGmw.Ep6vdyJM$RmKMxDNjtjLu4qs7ATHQq7kYKbnrIwlMbEEqrhLoOPAlIghIFf2br8OFKF8C76fZ.eKIwj7m1FSzDNyi/16Zm.";
        shell = pkgs.zsh;
    };

    zramSwap = {
        enable = true;
        memoryPercent = 200;
    };

    i18n.inputMethod = {
        enable = "fcitx5";
        fcitx5.addons = [pkgs.fcitx5-mozc];
    };

    fonts = {
        fonts = with pkgs; [

        # Sans(Serif) fonts
        noto-fonts-cjk-serif
        noto-fonts-cjk-sans
        noto-fonts-emoji

        # monospace fonts
        jetbrains-mono

        # nerdfonts
        (nerdfonts.override {fonts = ["NerdFontsSymbolsOnly"];})

        # Hackgen NF
        "${pkgs.fetchzip {
            url = "https://github.com/yuru7/HackGen/releases/download/v2.9.0/HackGen_NF_v2.9.0.zip";
            hash = "sha256-Lh4WQJjeP4JuR8jSXpRNSrjRsNPmNXSx5AItNYMJL2A=";
        }}"
        ];

        fontDir.enable = true;
        fontconfig = {
            defaultFonts = {
                serif = ["Noto Serif CJK JP" "Noto Color Emoji"];
                sansSerif = ["Noto Sans CJK JP" "Noto Color Emoji"];
                monospace = ["JetBrainMono Nerd Font" "Noto Color Emoji"];
                emoji = ["Noto Color Emoji"];
            };
        };
    };

    programs = {
        git = {
            enable = true;
        };
        
        neovim = {
            enable = true;
            defaultEditor = true;
            viAlias = tue;
            vimAlias = true;
        };

        starship = {
            enable = true;
        };

        zsh = {
            enable = true;
        };
    };
}
